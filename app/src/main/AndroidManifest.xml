<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dafou.privetest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AdminAct"
            android:exported="true" />
        <activity
            android:name=".waitres"
            android:exported="true" />
        <activity
            android:name=".waitress"
            android:label="@string/title_activity_waitress"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FinalAdmin"
            android:label="@string/title_activity_final_admin"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>